import * as crawler from '../src/crawl-handler';

describe('crawler tests', () => {
  // beforeAll(async () => {
  // }, 300000);

  // afterAll(async () => {
  // }, 100000);

  it('test dataflow-staging', async () => {}, 900000);
});
