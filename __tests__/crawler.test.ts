import * as crawler from '../src/crawler';

describe('crawler tests', () => {
  // beforeAll(async () => {
  // }, 300000);

  // afterAll(async () => {
  // }, 100000);

  it('test something', async () => {
    // await crawler.crawlLink('https://google.com');
  }, 100000);
});
